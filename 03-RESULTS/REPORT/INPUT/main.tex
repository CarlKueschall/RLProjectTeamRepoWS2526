\documentclass[11pt]{article}

\usepackage{times}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{url}
\usepackage{graphics}
\usepackage{graphicx}
\usepackage{color}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{subcaption}
\usepackage{caption}
\captionsetup{font=small,labelfont=bf}
\captionsetup[subfigure]{font=footnotesize,labelfont=normalfont}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{geometry}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage{xcolor}

% Geometry setup
\geometry{left=2.8cm,right=2.8cm,top=2.4cm,bottom=2.4cm,headheight=14pt}
\graphicspath{{./}{./figures/}{../figures/}}
\pagestyle{fancy}
\lhead{RL Hockey Project}
\chead{}
\rhead{\thepage}
\cfoot{}

\title{World-Model Based Reinforcement Learning for Hockey:\\
        DreamerV3 with 2-Phase Training Curriculum}
\author{Carl Kueschall\\
        University of T\"ubingen}
\date{Winter Semester 2025/26}

\begin{document}

\maketitle

% ============================================================================
% SECTION 1: INTRODUCTION
% ============================================================================
\section{Introduction}\label{sec:intro}

\textit{Note: Originally a two-person project; co-author exmatriculated. Work completed by remaining author.}

\medskip
The hockey environment~\cite{martius2024} presents a challenging continuous control problem in an adversarial setting. Two agents compete in an air-hockey game with 18-dimensional state observations encoding positions, velocities, and puck state, while each player controls 4 continuous action dimensions. Episodes last up to 250 timesteps with extremely sparse rewards: $+10$ for scoring, $-10$ for conceding, and $0$ otherwise. This sparsity makes credit assignment difficult---agents must learn complex behaviors (positioning, puck control, shooting, defending) from reward signals that occur only at episode boundaries.

We implement DreamerV3~\cite{hafner2024}, a world-model based reinforcement learning algorithm that addresses sparse rewards by learning entirely in ``imagination.'' The agent first learns a world model from real experience, then trains its policy through simulated rollouts in learned latent space. Our implementation builds upon the NaturalDreamer codebase\footnote{\url{https://github.com/InexperiencedMe/NaturalDreamer}}, with significant modifications for the hockey domain: Two-Hot Symlog encoding for sparse reward prediction, DreamSmooth~\cite{dreamsmooth2023} temporal reward smoothing, mixed opponent training, and a 2-phase curriculum that first saturates performance against fixed bots, then adds self-play from a strong checkpoint to achieve best results.

\textbf{Contributions:} DreamerV3 implementation with Two-Hot Symlog, DreamSmooth, mixed opponent training, and PFSP self-play---achieving 93.5\% combined win rate (90\% weak, 97\% strong) and top pre-tournament leaderboard performance.

% ============================================================================
% SECTION 2: METHODS
% ============================================================================
\section{Methods}\label{sec:method}

\subsection{DreamerV3: World-Model Based RL}\label{subsec:dreamer-method}

\subsubsection{Background}

DreamerV3~\cite{hafner2024} learns a world model that predicts future states and rewards from actions, then trains actor-critic policies entirely through imagined rollouts in learned latent space. This is particularly suited for sparse-reward environments: the world model propagates reward information backward through imagined trajectories, enabling credit assignment even when real rewards are rare. Our implementation adapts this for the hockey domain, addressing (1) extremely sparse goal rewards, (2) continuous 4D action space, and (3) adversarial dynamics.

\subsubsection{World Model Architecture}

The world model uses a Recurrent State Space Model (RSSM) combining deterministic and stochastic dynamics. The state $s_t = (h_t, z_t)$ consists of a deterministic GRU hidden state $h_t \in \mathbb{R}^{256}$ and stochastic latent $z_t$ (16 categorical variables, 16 classes each). Dynamics: $h_t = \text{GRU}(h_{t-1}, z_{t-1}, a_{t-1})$; prior $\hat{z}_t \sim p_\theta(z_t | h_t)$; posterior $z_t \sim q_\phi(z_t | h_t, \text{Enc}(o_t))$. KL divergence with free nats thresholding trains the model.

\textbf{Two-Hot Symlog.} MSE fails on hockey's sparse reward distribution $\{-10, 0, +10\}$. We use Two-Hot Symlog encoding~\cite{hafner2024}: $\text{symlog}(x) = \text{sign}(x) \cdot \ln(|x| + 1)$, discretizing into 255 bins. The two-hot encoding places mass on bins surrounding the target, enabling gradient flow. Applied to both reward and value prediction.

\subsubsection{Key Modifications for Hockey}

Table~\ref{tab:modifications} summarizes our domain-specific modifications. \textbf{DreamSmooth}~\cite{dreamsmooth2023} propagates goal rewards backward: $\tilde{r}_t = \alpha r_t + (1-\alpha)\tilde{r}_{t+1}$ with $\alpha = 0.5$, addressing temporal credit assignment for goals at episode boundaries.

\begin{table}[h]
\centering
\small
\begin{tabular}{lp{4.2cm}p{4.8cm}}
\toprule
\textbf{Modification} & \textbf{Problem Addressed} & \textbf{Implementation} \\
\midrule
Two-Hot Symlog & MSE fails on sparse rewards & 255 bins in symlog space \\
DreamSmooth & Temporal credit assignment & $\tilde{r}_t = \alpha r_t + (1-\alpha)\tilde{r}_{t+1}$, $\alpha=0.5$ \\
Mixed Opponents & Overfitting to single opponent & 50\% weak / 50\% strong per episode \\
Self-Play + PFSP & Limited diversity & 20-checkpoint pool, variance-weighted selection \\
\bottomrule
\end{tabular}
\caption{Key modifications from baseline DreamerV3 for the hockey domain.}
\label{tab:modifications}
\end{table}

\subsubsection{Behavior Learning}

\textbf{Actor:} Tanh-squashed Gaussian with $\sigma_{\min} = 0.1$. \textbf{Critic:} Two-Hot Symlog with slow EMA target ($\tau = 0.98$). \textbf{Lambda returns:} TD($\lambda$), $\lambda = 0.95$, $\gamma = 0.997$. \textbf{Value normalization:} Percentile-based scaling (5th/95th) for advantages. \textbf{Actor loss:}
\begin{equation}
\mathcal{L}_\text{actor} = -\mathbb{E}_{s \sim \text{imagine}} \left[ \frac{V^\lambda(s) - V(s)}{S} + \eta \cdot H[\pi(\cdot|s)] \right]
\end{equation}
with $\eta = 3 \times 10^{-4}$ fixed.

% ============================================================================
% SECTION 3: EXPERIMENTAL EVALUATION
% ============================================================================
\section{Experimental Evaluation}\label{sec:experiments}

\subsection{2-Phase Training Curriculum}

We use a 2-phase curriculum (Table~\ref{tab:training-phases}). \textbf{Phase 1} trains against mixed opponents (50\% weak, 50\% strong) \emph{without} self-play until performance saturates. We select a strong checkpoint at 180k gradient steps and resume in a new run. \textbf{Phase 2} adds self-play from episode 1: a pool of 20 past checkpoints with PFSP (variance mode) opponent selection and 50\% anchor / 50\% pool mix. The same learning rates and replay ratio (32) are used in both phases.

\begin{table}[h]
\centering
\small
\begin{tabular}{lccccc}
\toprule
\textbf{Phase} & \textbf{Time} & \textbf{RR} & \textbf{LR (Actor)} & \textbf{Opponents} & \textbf{Combined Win Rate} \\
\midrule
1: Mixed only & $\sim$39h & 32 & $1 \times 10^{-4}$ & Weak + Strong (no self-play) & 0\% $\rightarrow$ $\sim$95\% \\
2: + Self-Play & $\sim$20h & 32 & $1 \times 10^{-4}$ & Mixed + Pool (20 checkpoints) & $\sim$90\% $\rightarrow$ \textbf{93.5\%} \\
\bottomrule
\end{tabular}
\caption{2-phase training curriculum. RR = replay ratio. Phase 2 resumes from 180k checkpoint. Best checkpoint at 336k steps.\footnote{Both phases: RR=32, LR world/actor/critic $3\times10^{-4}$/$1\times10^{-4}$/$1\times10^{-4}$, DreamSmooth $\alpha$=0.5, mixed weak prob 0.5. Phase 1: warmup 100; Phase 2: warmup 200, pool 20, PFSP variance, weak\_ratio 0.5, save interval 500.}}
\label{tab:training-phases}
\end{table}

The top panel of Figure~\ref{fig:metrics-ablation} shows \texttt{eval/combined\_win\_rate} across both runs. The Phase 1 run (orange) saturates near 95\% by $\sim$200k steps. Phase 2 (red) begins at 180k already high; \texttt{combined\_win\_rate} does not show dramatic further gain because the metric is evaluated against fixed weak/strong bots, which Phase 1 already mastered. The benefit of self-play is therefore \emph{not} visible in this metric.

\subsection{Self-Play Metrics and Ablation Studies}

Self-play progress is visible in pool-vs-pool metrics (Figure~\ref{fig:metrics-ablation}, middle row): win rate vs. oldest third rises 50\%$\rightarrow$97\%; vs. newest third 0\%$\rightarrow$80\%; vs. pool overall 50\%$\rightarrow$87\%. Later checkpoints clearly beat earlier ones. Ablation studies (bottom) validate key modifications: DreamSmooth and Two-Hot Symlog each contribute to final performance.

\begin{figure}[htbp]
\centering
\vspace{-0.5em}
% Top: benchmark
\subcaptionbox{Combined win rate vs.\ weak+strong bots\label{fig:benchmark-composition}}{\includegraphics[width=0.95\linewidth]{benchmark_composition}}\\[0.3em]
% Row 1: Self-play metrics
\subcaptionbox{Win rate vs.\ oldest third\label{fig:sp-oldest}}{\includegraphics[width=0.31\linewidth]{selfplay_oldest_placeholder}}%
\hfill
\subcaptionbox{Win rate vs.\ newest third\label{fig:sp-newest}}{\includegraphics[width=0.31\linewidth]{selfplay_newest_placeholder}}%
\hfill
\subcaptionbox{Win rate vs.\ pool overall\label{fig:sp-pool}}{\includegraphics[width=0.31\linewidth]{selfplay_pool_placeholder}}\\[0.2em]
% Row 2: Ablations (2 panels)
\subcaptionbox{DreamSmooth vs.\ no DreamSmooth\label{fig:abl-ds}}{\includegraphics[width=0.47\linewidth]{ablation_dreamsmooth_placeholder}}%
\hfill
\subcaptionbox{Two-Hot vs.\ MSE\label{fig:abl-twohot}}{\includegraphics[width=0.47\linewidth]{ablation_twohot_placeholder}}
\vspace{-0.3em}
\caption{Top: Combined win rate vs.\ fixed bots across phases. Middle: Self-play metrics (win rate vs.\ pool thirds). Bottom: Ablation studies (DreamSmooth, Two-Hot). Placeholders replace with actual figures.}
\label{fig:metrics-ablation}
\end{figure}

\subsection{Final Benchmark Performance}

Table~\ref{tab:benchmark} presents evaluation of the best checkpoint (336k steps) over 100 games per opponent with deterministic policy (seed 42). Performance against the strong bot (97\%) exceeds the weak bot (90\%); we hypothesize the strong bot's predictable play is easier to exploit than the weak bot's suboptimal variance.

\begin{table}[h]
\centering
\begin{tabular}{lcccc}
\toprule
\textbf{Opponent} & \textbf{Win Rate} & \textbf{Tie Rate} & \textbf{Loss Rate} & \textbf{Avg Reward} \\
\midrule
Weak Bot & 90\% & 7\% & 3\% & +8.16 \\
Strong Bot & 97\% & 0\% & 3\% & +8.73 \\
\midrule
\textbf{Combined} & \textbf{93.5\%} & 3.5\% & 3.0\% & +8.45 \\
\bottomrule
\end{tabular}
\caption{Final benchmark (336k checkpoint, 100 games per opponent, deterministic, seed 42).}
\label{tab:benchmark}
\end{table}

% ============================================================================
% SECTION 4: DISCUSSION
% ============================================================================
\section{Discussion}\label{sec:discussion}

\textbf{Key Findings.} Two-Hot Symlog is critical---MSE fails on sparse $\{-10, 0, +10\}$ rewards. DreamSmooth accelerates learning by propagating goal signals backward. The 2-phase curriculum is effective: Phase 1 saturates performance against fixed bots without self-play; Phase 2 adds self-play from a strong checkpoint, and improvement is visible in self-play metrics (win rate vs. pool) rather than in \texttt{combined\_win\_rate} against fixed opponents. Mixed opponent training prevents overfitting to a single bot type. PFSP variance mode balances exposure to diverse opponents.

\textbf{Limitations.} Evaluation is limited to weak and strong bots; the tournament will reveal generalization. $\sim$59h total training constrains hyperparameter search. \textbf{Future Work.} Population-based training; model-based planning during evaluation; transfer to other adversarial domains.

% ============================================================================
% AI USAGE DECLARATION
% ============================================================================
\section{AI Usage Declaration}\label{sec:ai-usage}

\textbf{IDE Autocomplete:} Cursor AI used throughout. \textbf{Claude Code:} Learning (DreamerV3 internals, RL debugging) and routine tasks (plotting, W\&B, evaluation scripts). Core algorithmic decisions from careful analysis.

% ============================================================================
% REFERENCES
% ============================================================================
\bibliographystyle{abbrv}
\bibliography{main}

\end{document}
